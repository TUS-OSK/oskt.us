/**
 * テーマの型定義
 */

export default interface ThemeDeclaration {
  color: {
    primary: string
    primaryInverted: string
    primarySub: string
    secondary: string
    secondarySub: string
    secondaryInverted: string
  }
}
